{
  "name": "harness",
  "version": "0.1.0",
  "private": true,
  "description": "Visual workflow orchestrator for Claude Code skills",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "test": "vitest run",
    "start": "bash start.sh",
    "core": "pnpm --filter @harness/core",
    "gui": "pnpm --filter @harness/gui"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
