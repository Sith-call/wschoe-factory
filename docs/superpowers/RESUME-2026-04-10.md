# Resume Note — Orchestration Redesign, 2026-04-10

> 📍 **If you are a fresh Claude Code session opening this project, read THIS file first.**
> It tells you exactly where we are and what to do next.

## TL;DR

- **Where**: Multi-plan migration of app-factory orchestration from 3-tier nested subagents to official 2-tier skill-based model
- **Done**: Plan 1 (M1 inventory + M2 worker tools restriction)
- **Next**: Plan 2 (M3 — write 12 skills + 8 slash commands)
- **Blocker**: None. Ready to proceed.

## Context reload — read these files in order

1. **This file** — you're reading it
2. **`docs/superpowers/specs/2026-04-09-app-factory-orchestration-redesign.md`** — the authoritative spec (updated with M1 findings)
3. **`docs/superpowers/plans/2026-04-09-orchestration-m1-m2-inventory-and-tools.md`** — Plan 1 (completed, for pattern reference)
4. **`CLAUDE.md`** — project overview (note: this still describes the OLD 3-tier model; don't follow it yet — the whole point of this redesign is to replace it)
5. **`PRODUCT_LOOP.md`** — philosophy doc (also reflects OLD model)

Skip these unless needed:
- `/tmp/m1-*.txt` — M1 scratch files (may not exist in a fresh session; all findings are committed to the spec)

## Current state

### Git state
- **Main branch**: has commits through `9729d73`/`b1b25d2`/`460a8d4`/`6e4e07f` (spec, plan, gitignore)
- **Plan 1 worktree**: `.worktrees/plan1-m1-m2/` on branch `plan1/m1-m2-inventory-tools`
  - Contains 7 additional commits on top of main
  - Working tree clean (except pre-existing `.omc/state/*` and `.harness/worktrees/*` from other systems — ignore those)
- **Plan 1 branch is NOT merged to main yet.** User decision pending.

### Key commits on plan1 branch
```
751c84e docs: dev-reviewer reclassified as hidden orchestrator (late M1 finding)
d3efcac refactor(ait-team): add tools restriction to worker subagents
cc3712c refactor(agent-maker): add tools restriction to worker subagents
726f1a4 refactor(dev-team): add tools restriction to worker subagents
15872de refactor(design-team): add tools restriction to worker subagents
a2debad refactor(pm-agent): add tools restriction to worker subagents
2977525 docs: M1 inventory findings — resolve open questions in orchestration spec
```

### Environment (verified in M1 Task 6)
- Claude Code: 2.1.97 (≥ 2.1.32 required for Agent Teams ✅)
- `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` in `~/.claude/settings.json` ✅
- tmux 3.5a installed ✅
- M4 split-pane Teams mode ready

## What Plan 1 accomplished

**M1 (inventory + Open Questions resolution):**
- Read all ~36 worker files + 8 orchestrator files across 5 plugins
- Resolved OQ1: `maker-orchestrator` and `ait-orchestrator` are TRUE orchestrators
- Resolved OQ2: `agent-maker` master = `maker-orchestrator` itself
- Resolved OQ3: `ralph-phase-details.md` reference path valid
- Verified environment for Plan 3's Agent Teams test

**M2 (worker tools restriction):**
- Added `tools:` frontmatter to **36 worker subagents** across 5 plugins
- No worker has `Agent` in its tools list (structurally blocks nested spawn)
- 8 legacy orchestrator files still present (for rollback safety)

**Late finding:**
- `dev-reviewer.md` discovered as a hidden orchestrator (Task 17) — its body lists 12+ agents to launch across 8 "Review Layers". Spec updated to include it in deletion list. Interim: tools restriction stays, dev-reviewer behaves as single-worker review until Plan 4 replaces it with `dev-review` skill.

## What Plan 2 needs to do

Write the following to `docs/superpowers/plans/2026-04-10-orchestration-m3-skills.md`:

**12 skills to write** (spec Section 5.1 — updated count from 11 to 12):

| # | Skill | Source |
|---|---|---|
| 1 | `app-factory` | `plugins/pm-agent/agents/app-factory.md` body |
| 2 | `pm-orchestrate` | `plugins/pm-agent/agents/pm-orchestrator.md` body |
| 3 | `stitch-generate` | `plugins/design-team/agents/design-orchestrator.md` + existing `stitch-workflow` skill |
| 4 | `design-sync` | `plugins/design-team/agents/design-sync-lead.md` body |
| 5 | `dev-orchestrate` | `plugins/dev-team/agents/dev-orchestrator.md` body |
| 6 | `ralph-persona-loop` | **update existing skill** — add Score YAML, 6-evaluator structure, parallel-with-fallback |
| 7 | `ralph-design-loop` | minor updates to existing skill |
| 8 | `release-prep` | new (extracted from app-factory Stage 5) |
| 9 | `bugfix-coordinate` | `plugins/dev-team/agents/bugfix-coordinator.md` body |
| 10 | `dev-review` | **NEW** — `plugins/dev-team/agents/dev-reviewer.md` body (late M1 finding) |
| 11 | `maker-orchestrate` | `plugins/agent-maker/agents/maker-orchestrator.md` body |
| 12 | `ait-orchestrate` | `plugins/ait-team/agents/ait-orchestrator.md` body |

**8 slash commands** (spec Section 5.2 — updated count from 7 to 8):
- `/app-factory <idea>`
- `/pm-orchestrate <app-name>`
- `/stitch-generate <app-name>`
- `/design-sync <app-name>`
- `/dev-orchestrate <app-name>`
- `/ralph-loop <app-name>`
- `/release-prep <app-name>`
- `/dev-review <scope>` (NEW)

**Write Plan 2 in the same pattern as Plan 1**:
- Header with goal, architecture, tech stack
- File structure section
- Tasks grouped by skill (or logical chunk)
- Each task: exact file path, full content or precise step-by-step instructions
- Commit after each skill
- Self-review section
- Execution handoff note

**Use existing Plan 1 as a template for formatting**.

**Do NOT write the actual skill content in Plan 2 itself** — write INSTRUCTIONS for the implementer subagent to produce the skill. The skill body content should be derived from:
- Read the corresponding legacy orchestrator `.md` file (or the existing skill if updating)
- Transform the body into a skill recipe following spec Section 4 "Skill Anatomy" format
- Include prerequisites, execution steps, worker dispatch plan, gate verification, error handling, final state

## Critical constraints (from spec)

1. **Tier 2 subagents never get `Agent` in tools** — already enforced in Plan 1. Don't undo.
2. **Skills dispatch workers, workers never dispatch skills or other workers** — the core 2-tier rule.
3. **Stage handoff via file system + TodoWrite**, not text signals.
4. **Ralph Loop's 6 parallel evaluators** (not 5): persona×3 + design-visionary + ux-specialist + pm-executor(reviewer mode).
5. **Score YAML files** are separate from worker final messages (spec Section 8.6).
6. **`pm-executor` reviewer mode**: spawn prompt must omit `Edit` to structurally protect `prd.md`.
7. **`app-factory` does NOT spawn workers directly** — it only invokes other skills (spec Section 7.2).
8. **Stitch MCP, NotebookLM MCP**: loaded via MCP config, NOT in `tools:` array.

## Decisions already made (don't re-brainstorm)

From the completed brainstorming (Appendix B of spec):
1. Approach A (flatten to 2-tier)
2. `app-factory` = skill + slash command
3. Worker tools restriction structurally enforced
4. Legacy orchestrators deleted in future plan
5. `design-orchestrate` split into `stitch-generate` + `design-sync`
6. `bugfix-coordinator` → skill
7. Ralph Phase 4 = multi-worker dispatch (not single integrator)
8. Gate strictness: keyword grep (level B)
9. Ralph Phase 2: 6 parallel evaluators
10. Score blocks: separate YAML files
11. Optimistic parallel + auto sequential fallback
12. M4 test: Agent Teams (Teams) primary, git worktree fallback
13. M4 test app: "하루 감사 일기"
14. Migration: 2-PR split
15. `ORCHESTRATION_MODEL.md` ADR: create as separate file (Plan 4)

## Action for the new session

```
You are resuming the app-factory orchestration redesign.

1. Read this file: docs/superpowers/RESUME-2026-04-10.md
2. Read the spec: docs/superpowers/specs/2026-04-09-app-factory-orchestration-redesign.md
3. Read Plan 1 for pattern reference: docs/superpowers/plans/2026-04-09-orchestration-m1-m2-inventory-and-tools.md
4. Invoke superpowers:writing-plans skill
5. Write Plan 2 (M3 — 12 skills + 8 slash commands) to:
   docs/superpowers/plans/2026-04-10-orchestration-m3-skills.md
6. Commit Plan 2 on the same plan1 branch (or a new plan2 branch if user prefers)
7. Do NOT start executing Plan 2 in the same session unless context budget is very generous — the skill writing itself will consume a lot. Save execution for Plan 2's own session.
```

## If user wants to merge Plan 1 first

Plan 1 is complete and committed on the `plan1/m1-m2-inventory-tools` branch. To merge:

```bash
cd /Users/wschoe/project/plz-survive-jay
git checkout main
git merge --no-ff plan1/m1-m2-inventory-tools -m "Merge Plan 1: M1 inventory + M2 worker tools restriction"
```

Or via PR on GitHub if that's the preferred workflow.

The worktree at `.worktrees/plan1-m1-m2/` can be removed after merge:
```bash
git worktree remove .worktrees/plan1-m1-m2
```

## Status dashboard

```
Plan 0 (brainstorming + spec)     ✅ DONE (committed on main: 9729d73, b1b25d2)
Plan 1 (M1 + M2)                   ✅ DONE (branch: plan1/m1-m2-inventory-tools)
Plan 2 (M3 — 12 skills)            ⏳ TO WRITE in new session
Plan 3 (M4 — Teams test app)       ⏳ NOT STARTED
Plan 4 (M5+M6+M7)                  ⏳ NOT STARTED
```

Pipeline completeness: ~25% (spec + 1 of 4 plans executed)
